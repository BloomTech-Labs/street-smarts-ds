{
    "make": value,
    "model": value,
    "year": value,
    "state": value,
    "mileage": value,
    "price":value,
    "fuel type":value
}

# Function to query EPA data make and model (for CO2)
# Function to predict car price
# Function for the 5 year cost
# 
#
# The EPA query will be psycopg2 connection open, make query,
#   get query, take tuples into a df, 
# then use those CO2 values in our CO2 model for 5 yr cost to own


